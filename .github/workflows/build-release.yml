name: build-release
on: [push]
jobs:
  build-binary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get -y install build-essential
      - run: cd ssdp-advertiser
      - run: ./configure --prefix=/usr
      - run: make
